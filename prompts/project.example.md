# Project-specific guidance (example)

Only loaded when `bootstrap_mode = "rich"` and `bootstrap_extra_md` points here.

Use this file for **your** environment — not general Ordo knowledge.

Examples you might add:

- Prefer server `panthero` (id 4) for deploy jobs; `liono` is light-only.
- Production deploy cluster path: `/root/deploy-saas`.
- Never delete cluster id 15 (Monitoring) without asking twice.
- Default calendar timezone for this org: America/Chicago.

Keep this short. Long project notes burn tokens on every turn.
