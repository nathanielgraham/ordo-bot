# Example configuration for ordo-bot
# Copy this file to config.toml and edit the values.

# ------------------------------------------------------------------
# Ordo connection
# ------------------------------------------------------------------
ordo_ws_url = "wss://ordoscheduler.com/websocket"
ordo_token = "YOUR_ORDO_API_TOKEN_HERE"

# ------------------------------------------------------------------
# LLM (OpenAI-compatible endpoint)
# ------------------------------------------------------------------
llm_base_url = "http://localhost:11434/v1"
llm_api_key = "ollama"
llm_model = "llama3.2"

# Groq example:
# llm_base_url = "https://api.groq.com/openai/v1"
# llm_api_key = "gsk_..."
# llm_model = "openai/gpt-oss-20b"

# Per-call HTTP timeout. Retries default to 0 (fail fast — user retries).
llm_timeout_sec = 90
llm_max_retries = 0

# ------------------------------------------------------------------
# Agent context hygiene / bootstrap
# ------------------------------------------------------------------
max_history_messages = 24
tool_result_max_chars = 2500
chat_timeout_sec = 120

# minimal | standard | rich
bootstrap_mode = "standard"
bootstrap_docs = true
# bootstrap_playbook_path = "prompts/bootstrap.md"
# bootstrap_extra_md = "prompts/project.md"  # rich only

# ------------------------------------------------------------------
# Frontend WebSocket server
# ------------------------------------------------------------------
frontend_host = "127.0.0.1"
frontend_port = 8765

log_level = "INFO"
